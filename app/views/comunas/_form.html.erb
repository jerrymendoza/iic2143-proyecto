<%= form_with(model: comuna, local: true) do |form| %>
  <% if comuna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comuna.errors.count, "error") %> se prohibió guardar esta comuna:</h2>

      <ul>
      <% comuna.errors.full_messages.each do |message| %>
        <li><%= mensaje %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, "Nombre" %><br />
    <%= form.text_field :name, :class => 'uk-input uk-width-1-3' %>
  </div>
  <br />
  <div class='actions'>
      <%= form.submit "Aceptar", class: 'uk-button uk-button-primary'  %>
  </div>
<% end %>
